<?php

    $int = 12; // integer (cjelobrojne vrijednosti)
    $float = 1.34; // float (brojevi sa pokretnim zarezom)
    $string = "bozidar"; // string (niz znakova)
    $boolean = false; // bool (logičke vrijednosti)
    $array = [1,2,3]; // array (polje/niz)
    $object = new Klasa(); // object (objekt)
    $null = NULL; // NULL (nepostojeća vrijednost)

    class Klasa {

    }
    